#ifndef GLISY_SCENE_H
#define GLISY_SCENE_H

#include <glisy/math/vec3.h>

/**
 * Glisy Scene structure type.
 */

typedef struct glisy_scene glisy_scene;
struct glisy_scene {
  vec3 position;
};

#endif
